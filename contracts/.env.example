# Real token address for a mainnet deploy. Leave unset on testnet and the deploy
# script deploys the hostile MockUSDT instead; it refuses to do that on 677.
#
# Verified on chain — see docs/chain-677.md. There is a second, near-identical
# Tether-shaped token on 677 (BOUSDT, 0x118f7B25…); this is not it.
USDT_ADDRESS=0xaBabc7Ddc03e501d190C676BF3d92ef0e6e87a3C
